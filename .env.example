# OpenClaw Environment Variables (MoltBot custom fork)
# Copy this file to .env and fill in your values.
# .env is gitignored -- never commit real secrets.
#
# Env-source precedence (highest -> lowest):
# process env, ./.env, ~/.openclaw/.env, then openclaw.json `env` block.

# -----------------------------------------------------------------------------
# Gateway auth + paths
# -----------------------------------------------------------------------------
# Auto-generated on first gateway start (64-char hex string)
# Example generator: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token

# Optional alternative auth mode (use token OR password).
# OPENCLAW_GATEWAY_PASSWORD=change-me-to-a-strong-password

# Use 'lan' inside container (binds 0.0.0.0 inside); host port restricted to 127.0.0.1 via docker-compose
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_IMAGE=openclaw:local
OPENCLAW_CONFIG_DIR=/home/<user>/.openclaw
OPENCLAW_WORKSPACE_DIR=/home/<user>/clawd

# Optional path overrides (defaults shown for reference).
# OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json
# OPENCLAW_HOME=~

# Optional: import missing keys from your login shell profile.
# OPENCLAW_LOAD_SHELL_ENV=1
# OPENCLAW_SHELL_ENV_TIMEOUT_MS=15000

# -----------------------------------------------------------------------------
# Model provider API keys (set at least one)
# -----------------------------------------------------------------------------
# Option 1: Standard API key from console.anthropic.com (per-token billing, ToS-safe)
# ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_WITH_YOUR_API_KEY
#
# Option 2: Claude Max setup-token (flat rate, but risky for unattended agents)
# Anthropic has banned accounts using setup-tokens for autonomous loops (Jan 2026).
# ANTHROPIC_API_KEY=sk-ant-oat01-REPLACE_WITH_YOUR_SETUP_TOKEN
ANTHROPIC_API_KEY=

# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# Optional additional providers
# ZAI_API_KEY=...
# AI_GATEWAY_API_KEY=...

# GitHub (for git push via HTTPS token auth)
# Generate a fine-grained PAT at https://github.com/settings/tokens?type=beta
# Required permissions: Contents (read/write)
GITHUB_TOKEN=

# -----------------------------------------------------------------------------
# Local Models (Ollama)
# -----------------------------------------------------------------------------
# Set to enable Ollama auto-discovery
OLLAMA_API_KEY=ollama-local
# Override Ollama host (default: http://ollama:11434 via Docker DNS)
# OLLAMA_HOST=http://ollama:11434

# -----------------------------------------------------------------------------
# MCP Gateway
# -----------------------------------------------------------------------------
# Generate with: openssl rand -hex 32
# Used to authenticate openclaw-gateway -> mcp-gateway connections
MCP_GATEWAY_AUTH_TOKEN=REPLACE_WITH_GENERATED_HEX_TOKEN

# -----------------------------------------------------------------------------
# Volume Mounts (personal paths, never committed)
# -----------------------------------------------------------------------------
DOCKER_MCP_CONFIG=/mnt/c/Users/<WindowsUser>/.docker/mcp
TWITCH_WORKSPACE_DIR=/home/<user>/clawd-twitch
BUSINESS_DEV_REPO=/home/<user>/repos/SpokeToWork---Business-Development
TRANSCRIPTS_REPO=/home/<user>/repos/TranScripts
TRADING_DATA_DIR=/home/<user>/repos/openclaw/trading-data

# -----------------------------------------------------------------------------
# Channels (only set what you enable)
# -----------------------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...

# Twitch (bot account)
# IMPORTANT: Generate token while logged into Twitch as the BOT account, not your main account.
# Required scopes: chat:read chat:write user:write:chat channel:manage:broadcast
#   channel:manage:schedule moderator:manage:announcements clips:edit user:edit:broadcast
# Generate at https://twitchtokengenerator.com with all scopes above.
#
# NOTE: These tokens are stored in ~/.openclaw/.env (the config dir), NOT passed via
# docker-compose environment. The onRefresh callback auto-updates ~/.openclaw/.env
# when tokens are refreshed. Set initial values there, not here.
OPENCLAW_TWITCH_ACCESS_TOKEN=REPLACE_WITH_TWITCH_ACCESS_TOKEN
OPENCLAW_TWITCH_CLIENT_ID=REPLACE_WITH_TWITCH_CLIENT_ID
OPENCLAW_TWITCH_CLIENT_SECRET=
OPENCLAW_TWITCH_REFRESH_TOKEN=
# Token refresh metadata (auto-managed by onRefresh callback in ~/.openclaw/.env)
# OPENCLAW_TWITCH_EXPIRES_IN=
# OPENCLAW_TWITCH_OBTAINMENT_TIMESTAMP=

# Gmail
GMAIL_PERSONAL=you@gmail.com
GMAIL_BUSINESS=business@gmail.com

# -----------------------------------------------------------------------------
# Claude Web (optional, for web session auth)
# -----------------------------------------------------------------------------
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# -----------------------------------------------------------------------------
# Tools + voice/media (optional)
# -----------------------------------------------------------------------------
# BRAVE_API_KEY=...
# PERPLEXITY_API_KEY=pplx-...
# FIRECRAWL_API_KEY=...
# ELEVENLABS_API_KEY=...
# DEEPGRAM_API_KEY=...

# Forex / Stock Data
# Free API key from https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=

# -----------------------------------------------------------------------------
# OBS Studio
# -----------------------------------------------------------------------------
# OBS WebSocket server (built-in, OBS 28+). Enable in OBS -> Tools -> WebSocket Server Settings.
OBS_WS_HOST=host.docker.internal
OBS_WS_PORT=4455
OBS_WS_PASSWORD=
# Host-side launcher (run toolkit/host-scripts/obs_launcher.py on WSL2 host)
OBS_LAUNCHER_URL=http://host.docker.internal:8100
# Episode playback scene/source names (must match OBS scene setup)
OBS_MEDIA_SOURCE=EpisodeVideo
OBS_PLAYBACK_SCENE=Episode Playback
# Windows path prefix for renders dir
OBS_RENDERS_WIN_PREFIX=
# Twitch stream key (from Dashboard -> Settings -> Stream -> Primary Stream Key)
OBS_STREAM_KEY=
# Show flow scene names (must match pre-configured OBS scenes)
OBS_STARTING_SCENE=Starting Soon
OBS_STARTING_DURATION=120
OBS_INTRO_SCENE=Stream Intro
OBS_INTRO_SOURCE=IntroVideo
OBS_OUTRO_SCENE=Starting Soon
OBS_OUTRO_DURATION=30
